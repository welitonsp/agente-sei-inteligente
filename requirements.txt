# Fundacao tecnica (Etapa 2). Dependencias minimas.
pydantic>=2.7
pydantic-settings>=2.3
SQLAlchemy>=2.0
python-dotenv>=1.0
pypdf>=4.0
PyMuPDF>=1.23.0
fastapi>=0.100.0
uvicorn>=0.23.0
langgraph>=0.0.30
langchain-core>=0.1.0
langchain-google-genai>=1.0.0

# Integracao Google Agenda (Etapa 3) - import tardio em GoogleCalendarBackend.
# Necessario apenas para criar eventos reais; testes usam backend em memoria.
google-api-python-client>=2.0
google-auth>=2.0
google-auth-oauthlib>=1.0

# Automacao e extracao assistida (Etapa 4/5)
playwright>=1.40.0

# Camada de IA (Claude no controle) - import tardio em ClaudeProvider.
# Necessario apenas para o provedor real Claude; testes e modo echo nao usam.
anthropic>=0.40
google-generativeai>=0.5.0

# Desenvolvimento / testes
pytest>=8.0
